{% extends 'base.html.twig' %}

{% block title %}List of the dates{% endblock %}

{% block body %}
    <h1 style="text-align: center"> Next birthdays on 30 days</h1> <br>
    <div class="container">
        <table class="table">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nom</th>
                <th scope="col">Prénom</th>
                <th scope="col">Date</th>
            </tr>
            </thead>
            <tbody>
            {% for data in list %}
                <tr>
                    <th scope="row">{{ data.id }}</th>
                    <td>{{ data.Nom }}</td>
                    <td>{{ data.Prenom }}</td>
                    <td>{{ data.Date|date('Y-m-d') }}</td>
                </tr>
            {% endfor %}
            <a class="btn btn-info mt-2 text-white" href="{{ path('main') }}">Show</a>
{#            <a class="btn btn-danger mt-2 text-white" href="{{ path('main') }}">Back to list</a> PAGE PRINCIPALE LISTE SUR 30 JOURS #}
            </tbody>
        </table>
    </div>
{% endblock %}